# ScopeAnalyser — Autonomous Session Plan (2026-06-03 21:19)

User went to sleep, asked me to continue through the roadmap autonomously,
skip any blocking questions, and ask remaining questions at the end.

## Plan

1. **Skip Phase 2 (OversampledChannel)** — needs a real PLC with an
   oversampling terminal or a fast-task ARRAY symbol. Can't be validated
   without one. Will revisit when a real PLC connection is available.
2. **Phase 3 (Analyser)** — full implementation:
   - FormulaEngine (hand-rolled parser, evaluator)
   - Function library (Filter, Integral, Derivative, Mean, RMS, Min, Max,
     Shift, Abs, Sqrt, Log, Sin, Cos)
   - AnalyserWidget UI (formula editor + Ctrl+Space autocomplete + help
     panel + multi-pane plot with toggleable channels)
   - End-to-end tests
3. **Phase 4 (Converter)**:
   - CSV source (QXlsx still blocked on Ubuntu 25.10 GuiPrivate, defer Excel)
   - Drag-mapping UI with role per column
   - Profile save/load
   - Tests
4. **README + execution log update** so the user can pick up cleanly.
